const fs = require('fs'); const path = require('path'); function walk(dir) { let results = []; const list = fs.readdirSync(dir); list.forEach(file => { file = path.resolve(dir, file); const stat = fs.statSync(file); if (stat && stat.isDirectory()) { if (!file.includes('node_modules') && !file.includes('.git') && !file.includes('dist')) results = results.concat(walk(file)); } else { const content = fs.readFileSync(file, 'utf8'); if (/india.*?explorer/i.test(content)) results.push(file); } }); return results; } console.log(walk('.'));
